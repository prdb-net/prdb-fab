# Secrets and local configuration
*.local
.env
.env.*
!.env.example

# Local agent instructions (not part of the public repository)
.claude/settings.local.json
CLAUDE.local.md

# Issue tracking and planning notes, kept out of the public repository
.scratch/
