/** What is configured, which every one of the four forms changes. */
export const connectionsKey = ['connections'] as const

/** The indexer rows, which the indexer form adds to one at a time. */
export const indexersKey = ['connections', 'indexers'] as const
